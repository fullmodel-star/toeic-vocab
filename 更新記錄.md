# 更新記錄 — 多益單字 TOEIC

> 線上版：<https://fullmodel-star.github.io/toeic-vocab/>

---

## 2026-07-10 — 小朋友實測建議 4 改 + 家長雲端後台

- **測驗防偷看**：作答中鎖住底部導覽（只剩 ✕ 結束測驗），不能中途跳去單元頁對答案。
- **固定 50 題**：移除題數選單，測驗一律最完整 50 題（範圍可選全部或單一單元）。
- **綜合測驗**：每過完 4 個單元解鎖一關，用拼字考 100 題、≥80% 過關 🏆。
- **錯題本升級**：改成底部獨立分頁，可依 主題／詞性／單元／級別 分類複習。
- **家長後台（雲端）**：新增 engSync 回報模組，進度自動上傳自家 Cloudflare Worker，家長一個網址看兩個小孩各科進度。
- 修正 `_install_inject.py` 漏 `</script>` 的老問題；同步模組插入點統一到「`</body>` 前最後一個 `</script>` 之後」。
- 注入器：`_k28_inject.py`（四大功能）、`_parentsync_inject.py`（雲端回報）；後台在 `_家長後台/`。
